# Mote ROS Noetic driver

```
docker build --progress=plain -f mote-ros-noetic/Dockerfile -t mote-ros-noetic .
                 ROBOT_IP=192.168.0.130 docker compose -f mote-ros-noetic/docker-compose.yml up
```
